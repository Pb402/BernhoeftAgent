import React from "react";
import ReactDOM from "react-dom/client";
import BernhoeftAgent from "./BernhoeftAgent.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <BernhoeftAgent />
  </React.StrictMode>
);
